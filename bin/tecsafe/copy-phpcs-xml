#!/usr/bin/env sh

# shellcheck source=bin/config
. "$(dirname "$0")/../config"

ARGS="$@"
if [ $# -eq 0 ] ; then
    ARGS=""
fi

docker run --rm --init \
    -v "$DIR":/app:delegated \
    "$DEVTOOLS_IMAGE" \
    cp -v /phpcs/phpcs.xml.dist /app/phpcs.xml.dist
