#!/usr/bin/env sh

# Do not modify this file - this file is created by bin/wrapper/dist-sync and will be overriden every time dist-sync is invoked.
# see https://gitlab.brainbits.net/docker/dist/-/blob/master/README.md

. "$(dirname $0)/../config"

variant="${STATIC_ANALYSIS:=phpstan}"

printf "${check}>>> running ${check_name}static-analysis${check} (%s) check on ${check_name}%s${reset}\n" "${variant}" "${NAME}"

"${BIN_DIR}/check/_variant/static-analysis-${variant}" $*
